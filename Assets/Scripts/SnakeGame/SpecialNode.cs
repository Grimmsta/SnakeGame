using UnityEngine;
using System.Collections;

namespace Grimm
{
    public class SpecialNode : Node
    {
        
        public SpecialNode(bool _walkable, Vector3 _worldPosition, int x, int y) : base(_walkable, _worldPosition, x, y)
        {
            
        }


    }
}
